<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Bowler Submitted</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/static/output.css" rel="stylesheet">
  </head>
  <body class="h-screen flex flex-col items-center justify-center">
   {{/* . context is the entire form. r(equest).Form */}}
   <h2 class="text-xl">Thank you, {{ index .submitter 0 }} for your picks!</h2><br>
   <h3 class="text-lg">Your picks are:</h3>
   {{/* Loop through the picks and show */}}
   {{ range $key, $value := . }}
      {{ if eq $key "submitter" }}
      {{/* skip the submitter field */}}
      {{ else }}
        <p class="text-base">{{ $key }}: {{ index $value 0 }}</p>
      {{ end }}
   {{ end }}
   <p class="text-blue-600"><a class="text-sm" href="/">Submit more picks</a></p>
  </body>
</html>
